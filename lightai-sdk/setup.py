from setuptools import setup, find_packages

setup(
    name="lightai",
    version="0.1.0",
    description="Function-level instrumentation SDK for LightAI",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)
